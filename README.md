# DTL
Dark Triad Luncher

## Install guide:

Debian:
`apt install curl -y`

Fedora:
`dnf install curl -y`

Arch:
`pacman install cur -y`

Installer:

```
curl -fsSL https://raw.githubusercontent.com/Vaynnore-r/DTL/main/install.sh | bash
```
